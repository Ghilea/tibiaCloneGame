import fs from "fs";
import path from "path";
const root = process.cwd();
const target = path.join(root, "apps", "client", "src", "game", "NativeWorldRenderer.tsx");
function read(file) { return fs.readFileSync(file, "utf8"); }
function write(file, text) { fs.mkdirSync(path.dirname(file), { recursive: true }); fs.writeFileSync(file, text); }
function replaceOnce(text, before, after, label) { const index = text.indexOf(before); if (index < 0) throw new Error(`V31B.4 exact anchor not found: ${label}. No files were written.`); if (text.indexOf(before, index + before.length) >= 0) throw new Error(`V31B.4 anchor for ${label} was ambiguous. No files were written.`); return text.slice(0, index) + after + text.slice(index + before.length); }
function ensureContains(text, needle, label) { if (!text.includes(needle)) throw new Error(`V31B.4 safety check failed: missing ${label}.`); }
if (!fs.existsSync(target)) throw new Error(`Could not find ${target}. Run this from the repository root.`);
let text = read(target);
ensureContains(text, "// TIBIAGAME_NATIVE_RENDERER_V31_B_3_4", "V31B.3.4 baseline marker");
ensureContains(text, "data-native-world-renderer=\"v31b.3.4\"", "V31B.3.4 canvas marker");
text = replaceOnce(text, "// TIBIAGAME_NATIVE_RENDERER_V31_B_3_4\n", "// TIBIAGAME_NATIVE_RENDERER_V31_B_3_4\n// TIBIAGAME_NATIVE_RENDERER_V31_B_4\n", "version marker");
text = replaceOnce(text, "type NativeRoofRegion = {\n  id: string;\n  floor: number;\n  minX: number;\n  minZ: number;\n  maxX: number;\n  maxZ: number;\n};\n", "type NativeRoofRegion = {\n  id: string;\n  floor: number;\n  minX: number;\n  minZ: number;\n  maxX: number;\n  maxZ: number;\n};\n\ntype NativeGatheringPreview = {\n  nodeId: string;\n  position: Position;\n  label: string;\n  startedAt: number;\n  durationMs: number;\n};\n", "NativeGatheringPreview type");
text = replaceOnce(text, "  private updateGroundAndResources(\n    world: WorldState,\n    floor: number,\n    local: Position,\n  ) {\n    const signature = [\n      world.revision,\n      world.streamRegionRevision,\n      floor,\n    ].join(\":\");\n", "  private updateGroundAndResources(\n    world: WorldState,\n    floor: number,\n    local: Position,\n    now: number,\n    gathering: NativeGatheringPreview | null,\n  ) {\n    const activeGatheringKey =\n      gathering && now < gathering.startedAt + gathering.durationMs\n        ? gathering.nodeId\n        : \"\";\n    const signature = [\n      world.revision,\n      world.streamRegionRevision,\n      floor,\n      activeGatheringKey,\n    ].join(\":\");\n", "ground/resource signature block");
text = replaceOnce(text, "      if (node.kind === \"copper_vein\") {\n        const target = node.available\n          ? copperAuthored\n          : copperDepletedAuthored;\n        target.push([\n          x,\n          0,\n          z,\n          1,\n          1,\n          1,\n          transform[6],\n        ]);\n        continue;\n      }\n", "      const gatheringActive = Boolean(\n        gathering\n        && gathering.nodeId === node.id\n        && now < gathering.startedAt + gathering.durationMs,\n      );\n\n      if (node.kind === \"copper_vein\") {\n        const renderAvailable = node.available || gatheringActive;\n        const target = renderAvailable\n          ? copperAuthored\n          : copperDepletedAuthored;\n        target.push([\n          x,\n          0,\n          z,\n          1,\n          1,\n          1,\n          transform[6],\n        ]);\n\n        if (renderAvailable) {\n          const pulse = gatheringActive\n            ? 1 + Math.sin(now * 0.014) * 0.08\n            : 1;\n          copper.push([\n            x - 0.18,\n            0.24,\n            z + 0.14,\n            0.74 * pulse,\n            1.08 * pulse,\n            0.74 * pulse,\n            transform[6] + 0.3,\n          ]);\n          copper.push([\n            x + 0.22,\n            0.29,\n            z - 0.08,\n            0.62 * pulse,\n            0.94 * pulse,\n            0.62 * pulse,\n            transform[6] + 1.4,\n          ]);\n          copper.push([\n            x + 0.05,\n            0.38,\n            z + 0.2,\n            0.54 * pulse,\n            0.86 * pulse,\n            0.54 * pulse,\n            transform[6] + 2.25,\n          ]);\n        }\n        continue;\n      }\n", "copper vein visibility block");
text = replaceOnce(text, "        resourceCopper: new THREE.MeshStandardMaterial({\n          color: \"#b86b3c\",\n          emissive: \"#5c2413\",\n          emissiveIntensity: 0.34,\n          metalness: 0.28,\n          roughness: 0.58,\n        }),\n", "        resourceCopper: new THREE.MeshStandardMaterial({\n          color: \"#dd8b4f\",\n          emissive: \"#9b431b\",\n          emissiveIntensity: 0.88,\n          metalness: 0.42,\n          roughness: 0.4,\n          toneMapped: false,\n        }),\n", "resource copper material");
text = replaceOnce(text, "  update(\n    world: WorldState,\n    map: MapView,\n    floor: number,\n    local: Position,\n    visualLocal: THREE.Vector3 | null,\n    actorManager: NativeActorManager,\n    daylight: number,\n    now: number,\n  ) {\n    this.updateGroundAndResources(world, floor, local);\n", "  update(\n    world: WorldState,\n    map: MapView,\n    floor: number,\n    local: Position,\n    visualLocal: THREE.Vector3 | null,\n    actorManager: NativeActorManager,\n    daylight: number,\n    now: number,\n    gathering: NativeGatheringPreview | null,\n  ) {\n    this.updateGroundAndResources(world, floor, local, now, gathering);\n", "dynamic scene manager update signature");
text = replaceOnce(text, "  const hoverRef = useRef<HTMLDivElement>(null);\n  const onReadyRef = useRef(onReady);\n", "  const hoverRef = useRef<HTMLDivElement>(null);\n  const gatheringRef = useRef<NativeGatheringPreview | null>(null);\n  const gatheringOverlayRef = useRef<HTMLDivElement>(null);\n  const gatheringFillRef = useRef<HTMLDivElement>(null);\n  const gatheringLabelRef = useRef<HTMLDivElement>(null);\n  const onReadyRef = useRef(onReady);\n", "renderer refs");
text = replaceOnce(text, "      const interactAtPointer = (event: PointerEvent | MouseEvent) => {\n        const position = pointerTile(event);\n        if (!position) return;\n        activatePointerTarget(targetAt(position), event);\n      };\n\n      const hideHover = () => {\n", "      const interactAtPointer = (event: PointerEvent | MouseEvent) => {\n        const position = pointerTile(event);\n        if (!position) return;\n        activatePointerTarget(targetAt(position), event);\n      };\n\n      const hideGatheringOverlay = () => {\n        if (gatheringOverlayRef.current) {\n          gatheringOverlayRef.current.style.display = \"none\";\n        }\n      };\n\n      const beginGatheringPreview = (\n        target: Extract<PointerTarget, { kind: \"resource\" }>,\n      ) => {\n        if (!target.available) return;\n        const current = gatheringRef.current;\n        const startedAt = performance.now();\n        if (\n          current\n          && current.nodeId === target.id\n          && startedAt < current.startedAt + current.durationMs\n        ) {\n          return;\n        }\n        const label = target.resourceKind === \"copper_vein\"\n          ? \"Mining copper vein...\"\n          : `Gathering ${humanizeId(target.resourceKind)}...`;\n        gatheringRef.current = {\n          nodeId: target.id,\n          position: target.position,\n          label,\n          startedAt,\n          durationMs: target.resourceKind === \"copper_vein\" ? 1350 : 950,\n        };\n      };\n\n      const hideHover = () => {\n", "gathering preview helpers");
text = replaceOnce(text, "        | { kind: \"npc\"; position: Position; id: string; label: string }\n        | { kind: \"creature\"; position: Position; id: string; label: string }\n        | { kind: \"player\"; position: Position; id: string; label: string }\n        | { kind: \"resource\"; position: Position; id: string; label: string; available: boolean }\n        | { kind: \"door\"; position: Position; id: string; label: string }\n", "        | { kind: \"npc\"; position: Position; id: string; label: string }\n        | { kind: \"creature\"; position: Position; id: string; label: string }\n        | { kind: \"player\"; position: Position; id: string; label: string }\n        | { kind: \"resource\"; position: Position; id: string; label: string; available: boolean; resourceKind: string }\n        | { kind: \"door\"; position: Position; id: string; label: string }\n", "pointer target resource type");
text = replaceOnce(text, "        if (resource) {\n          const name = humanizeId(resource.kind);\n          return {\n            kind: \"resource\",\n            position: resource.position,\n            id: resource.id,\n            label: resource.available ? `Use ${name}` : `${name} \u00b7 Depleted`,\n            available: resource.available,\n          };\n        }\n", "        if (resource) {\n          const name = humanizeId(resource.kind);\n          const action = resource.kind === \"copper_vein\" ? \"Mine\" : \"Use\";\n          return {\n            kind: \"resource\",\n            position: resource.position,\n            id: resource.id,\n            label: resource.available ? `${action} ${name}` : `${name} \u00b7 Depleted`,\n            available: resource.available,\n            resourceKind: resource.kind,\n          };\n        }\n", "pointer target resource label");
text = replaceOnce(text, "          case \"resource\":\n            input.interactAt(target.position);\n            return;\n", "          case \"resource\":\n            beginGatheringPreview(target);\n            input.interactAt(target.position);\n            return;\n", "resource activation");
text = replaceOnce(text, "          activeDynamicSceneManager.update(\n            world,\n            world.map,\n            initialLocal.position.z,\n            initialLocal.position,\n            initialVisualLocal,\n            activeActorManager,\n            initialEnvironment.daylight,\n            initialNow,\n          );\n", "          activeDynamicSceneManager.update(\n            world,\n            world.map,\n            initialLocal.position.z,\n            initialLocal.position,\n            initialVisualLocal,\n            activeActorManager,\n            initialEnvironment.daylight,\n            initialNow,\n            gatheringRef.current,\n          );\n", "initial dynamic update call");
text = replaceOnce(text, "          activeDynamicSceneManager.update(\n            world,\n            map,\n            floor,\n            local.position,\n            visualLocal,\n            activeActorManager,\n            environment.daylight,\n            now,\n          );\n          dynamicWorkMs = performance.now() - dynamicWorkStartedAt;\n\n          camera.position.set(\n", "          activeDynamicSceneManager.update(\n            world,\n            map,\n            floor,\n            local.position,\n            visualLocal,\n            activeActorManager,\n            environment.daylight,\n            now,\n            gatheringRef.current,\n          );\n          dynamicWorkMs = performance.now() - dynamicWorkStartedAt;\n\n          camera.position.set(\n", "runtime dynamic update call");
text = replaceOnce(text, "          textures.water.offset.set(\n            now * 0.000032,\n            now * 0.000019,\n          );\n\n          if (positionRef.current) {\n", "          textures.water.offset.set(\n            now * 0.000032,\n            now * 0.000019,\n          );\n\n          const gatheringPreview = gatheringRef.current;\n          if (\n            gatheringPreview\n            && gatheringOverlayRef.current\n            && gatheringFillRef.current\n            && gatheringLabelRef.current\n          ) {\n            const elapsed = now - gatheringPreview.startedAt;\n            const progress = THREE.MathUtils.clamp(\n              elapsed / gatheringPreview.durationMs,\n              0,\n              1,\n            );\n            const overlay = gatheringOverlayRef.current;\n            const rect = canvas.getBoundingClientRect();\n            const screenPosition = new THREE.Vector3(\n              gatheringPreview.position.x + 0.5,\n              1.18,\n              gatheringPreview.position.y + 0.5,\n            ).project(camera);\n            if (screenPosition.z > -1 && screenPosition.z < 1) {\n              overlay.style.display = \"block\";\n              overlay.style.left = `${rect.left + (screenPosition.x * 0.5 + 0.5) * rect.width}px`;\n              overlay.style.top = `${rect.top + (-screenPosition.y * 0.5 + 0.5) * rect.height - 18}px`;\n              gatheringFillRef.current.style.width = `${Math.round(progress * 100)}%`;\n              gatheringLabelRef.current.textContent = gatheringPreview.label;\n            } else {\n              overlay.style.display = \"none\";\n            }\n\n            const liveNode = world.resourceNodes.get(gatheringPreview.nodeId);\n            if (\n              progress >= 1\n              && (!liveNode || !liveNode.available || elapsed >= gatheringPreview.durationMs + 260)\n            ) {\n              gatheringRef.current = null;\n              hideGatheringOverlay();\n            }\n          } else {\n            hideGatheringOverlay();\n          }\n\n          if (positionRef.current) {\n", "gathering overlay render");
text = replaceOnce(text, "        }\n\n        const rendererWorkStartedAt = performance.now();\n", "        } else {\n          hideGatheringOverlay();\n        }\n\n        const rendererWorkStartedAt = performance.now();\n", "gathering overlay hide else");
text = replaceOnce(text, "      <div\n        ref={hoverRef}\n        className=\"ground-loot-tooltip\"\n        style={{\n          display: \"none\",\n          position: \"fixed\",\n          pointerEvents: \"none\",\n          zIndex: 50,\n        }}\n      />\n", "      <div\n        ref={hoverRef}\n        className=\"ground-loot-tooltip\"\n        style={{\n          display: \"none\",\n          position: \"fixed\",\n          pointerEvents: \"none\",\n          zIndex: 50,\n        }}\n      />\n      <div\n        ref={gatheringOverlayRef}\n        style={{\n          display: \"none\",\n          position: \"fixed\",\n          transform: \"translate(-50%, -100%)\",\n          pointerEvents: \"none\",\n          zIndex: 55,\n          minWidth: 132,\n          padding: \"6px 8px\",\n          borderRadius: 10,\n          background: \"rgba(8, 12, 10, 0.88)\",\n          border: \"1px solid rgba(212, 167, 94, 0.75)\",\n          boxShadow: \"0 6px 18px rgba(0, 0, 0, 0.35)\",\n          backdropFilter: \"blur(2px)\",\n        }}\n      >\n        <div\n          ref={gatheringLabelRef}\n          style={{\n            marginBottom: 4,\n            color: \"#f3dec0\",\n            fontSize: 12,\n            lineHeight: 1.2,\n            textAlign: \"center\",\n            whiteSpace: \"nowrap\",\n          }}\n        >\n          Mining...\n        </div>\n        <div\n          style={{\n            width: 132,\n            height: 8,\n            borderRadius: 999,\n            overflow: \"hidden\",\n            background: \"rgba(255, 255, 255, 0.12)\",\n            border: \"1px solid rgba(255, 255, 255, 0.09)\",\n          }}\n        >\n          <div\n            ref={gatheringFillRef}\n            style={{\n              width: \"0%\",\n              height: \"100%\",\n              borderRadius: 999,\n              background: \"linear-gradient(90deg, #a95a24 0%, #d28a49 50%, #f0ba6d 100%)\",\n              boxShadow: \"0 0 10px rgba(230, 150, 72, 0.5)\",\n            }}\n          />\n        </div>\n      </div>\n", "gathering overlay JSX");
text = replaceOnce(text, "        data-native-world-renderer=\"v31b.3.4\"\n", "        data-native-world-renderer=\"v31b.4\"\n", "canvas version tag");
text = replaceOnce(text, "      \"NATIVE WORLD V31B.3.4 active \u00b7 full GPU prewarm + frame profiler \u00b7 raw Three.js\",\n", "      \"NATIVE WORLD V31B.4 active \u00b7 copper clarity + gather progress preview \u00b7 raw Three.js\",\n", "console banner");
if (process.argv.includes("--check")) { console.log("V31B.4 compatibility check passed. No files were changed."); console.log("  V31B.3.4 baseline marker: found"); console.log("  copper vein clarity overlay crystals: ready"); console.log("  local gather progress preview: ready"); console.log("  copper node hold-visible during gather: ready"); process.exit(0); }
write(target, text);
console.log("Applied TibiaGame V31B.4 copper gathering progress preview.");
console.log("Changed:");
console.log("  apps/client/src/game/NativeWorldRenderer.tsx");
console.log("");
console.log("Run:");
console.log("  npm run check");
console.log("  npm run build");
