# V36.89 NPC Service Variants

NPC appearance is selected by authoritative `NpcView.service`.

- shop -> merchant
- depot -> vaultkeeper
- spell_trainer -> arcanist
- craft_trainer -> artificer

The existing default NPC actor remains the fallback for unknown future services.
All four variants use an eight-direction six-frame Idle animation.
