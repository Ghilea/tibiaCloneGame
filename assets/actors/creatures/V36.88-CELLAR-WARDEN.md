# V36.88 Cellar Warden + Area Telegraph

Cellar Warden is now a full eight-direction actor.

The existing AreaTelegraph protocol path is also made visible generically:
warning tiles use the server radius and duration, and the source creature faces
the telegraphed position and starts its generic Attack animation.

No warden_slam-specific rendering branch is introduced.
