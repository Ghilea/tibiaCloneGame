# V36.86 Swamp Creature Expansion

Adds full eight-direction Aldoria v1 sprite sets for:

- `reed_stalker`
- `fen_brute`

Both creatures use only `actors/creatures/index.json`, `actor.json`, and PNG
atlases. No creature-specific runtime branch is required.

Direction rows remain:

`N, NE, E, SE, S, SW, W, NW`
